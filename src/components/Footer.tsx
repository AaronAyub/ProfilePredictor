import { Container } from '@material-ui/core'

function Footer (): JSX.Element  {
    return (
        <Container>
            This website is not associated with Demografix ApS, which provides the services used for predictions.
        </Container>
    )
}

export default Footer